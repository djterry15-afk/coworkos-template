# CLAUDE.md
*Workstation: Markets — an EXAMPLE domain (a subject-area) for building a grounded macro/markets view.*
*Read ROOT CLAUDE.md fully before reading this. Read this fully before any project files.*

---

## WHAT THIS WORKSTATION IS FOR

*(Example.)* The domain where you build and maintain a grounded view of a markets question — *acquire*
evidence, *organize* it, *monitor* the data that matters, and *conclude*. This is the template's **composed**
example: a single project holds a wiki and two different tier-4 workflows, demonstrating that one
subject-area domain carries several workflow *shapes* (a shape is a property of a workflow, not of a domain).

---

## PROJECT MAP

**market-thesis** — `Reasoning · Active`. A standing markets thesis. Beneath it: an `llm-wiki/` that
*organizes* sourced knowledge, a `research-factory/` (tier-4, exploratory fan-in) that *acquires* new
external evidence into briefs, and a `stress-monitor/` (tier-4, deterministic monitor) that *watches* the
metrics the research identified. The verdict lives in the project `CONTEXT.md`.
→ Folder: `market-thesis/`

*(One composed project carries the teaching here. A real domain would hold several projects — this template
puts the acquire → organize → monitor → conclude loop inside one so the seams are visible in a single place.)*

---

## ROUTING TABLE

| Task | Navigate to | Load | Skip |
|---|---|---|---|
| Reasoning toward the thesis / a verdict | market-thesis/ | This file + CONTEXT.md → project files | other workstations |
| Acquiring new external evidence (a run) | market-thesis/research-factory/ | the factory's own files (production mode) | project synthesis |
| Running the stress monitor (a dated run) | market-thesis/stress-monitor/ | the monitor's own files (production mode) | project synthesis |
| Looking something up in organized sources | market-thesis/llm-wiki/ | the wiki's `index.md` | the factories |

---

## NAVIGATION SEQUENCE

1. ROOT CLAUDE.md (done) → 2. ROOT CONTEXT.md → 3. this file → 4. Markets CONTEXT.md →
5. the project's CLAUDE.md + CONTEXT.md → 6. begin work.
