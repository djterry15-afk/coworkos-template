# signal moments + direction — sample-deleting-code
*ILLUSTRATIVE. The `01_collect` output: verbatim moments pulled from the snapshot, then the angle the
human set at the gate (the U-curve peak).*

---

## Signal moments (extracted, verbatim from the snapshot)

- "Spent two hours pulling one 300-line function apart. Net change: **−180 lines**."
- "The bug I'd been chasing for a week was just gone — it had been hiding in a branch I deleted."
- "Each small function was boring on its own. Boring turned out to be the point."

## Direction (the angle — set by the human at the `01` gate)

**Angle:** the win wasn't the cleverness I added, it was the code I was finally able to *delete*. Smaller
pieces made a hidden bug obvious by removing the place it could hide.

*(This angle emerged from the pulled material — it was not handed down from tier-3. The brief-in approach
could not have produced it; the seed was only a topic.)*
