# manifest — sample-deleting-code
*ILLUSTRATIVE sample run. Shows the shape end-to-end; the content is invented, not a real post.*

```
seed:
  from:            entry-prompt
  topic:           what I noticed refactoring a tangled script into small functions
  platform-intent: [linkedin]

post:
  slug:        sample-deleting-code
  backlog-ref: one-off

sources:
  declared[]:
    - path:     (none — this is a reflection from direct experience)
  exploratory[]:
    - scope:       (the operator's own notes / commit history)
      looking-for: the concrete before/after moment that changed the thinking

provenance[]:
  - source:       work-journal note (illustrative)
    fetched-to:   01_collect/sample-deleting-code/snapshot-journal.md
    fetched-when: <date>
    source-state: settled
```

*No angle here. The angle was set at the `01_collect` gate — see `signal-moments-and-direction.md`.*
