# CONTEXT.md
*Project: content-project — the live synthesis (L1-as-synthesis at the project tier).*

> **STOP:** read this project's `CLAUDE.md` before this file.

---

## CURRENT STATE

*(Placeholder — this is where your real synthesis lives.)* The current positioning, audience, durable
voice, and any live unresolved strategy question. Keep it current; the truth layer must never lag the work.

## THE TIER-4 FACTORY

`content-factory/` is the production workflow beneath this project — the **content-production
(staged-pipeline + hub)** archetype. It takes one seed (a piece idea) → discovers the angle from the
material at the `01_collect` gate → drafts a platform-neutral core → renders to one or more platform sinks
at the hub. Reasoning about *what to publish* stays here at tier-3; the factory owns planning, research, and
rendering. See `content-factory/CLAUDE.md`.

## IDEA BACKLOG (optional)

*(Optional.)* A loose list of piece ideas (seeds) to feed the factory. A seed is a topic, not a settled
angle — the angle is set in-pipeline.
