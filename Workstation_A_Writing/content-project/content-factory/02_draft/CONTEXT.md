# CONTEXT.md — 02_draft (stage contract)
*Job: **execute the angle set at the `01` gate** into ONE platform-neutral core draft. The angle was decided from the material (in `direction.md`) — this stage renders it; the human edits **craft**, not the angle.*

---

## INPUTS
- `01_collect/output/<slug>/direction.md` — the chosen angle (set at the `01` gate).
- `01_collect/output/<slug>/signal-moments.md` (+ `snapshot.md` reachable for texture).
- `_config/voice-core.md` — platform-neutral voice + Hook/Body/Close shape.
- `_config/positioning.md` — the slim who's-speaking anchor.

## PROCESS
1. **Execute the angle** (`direction.md`) into a platform-neutral core: Hook → Body → Close in core voice. Pull felt texture from `snapshot.md` directly, not paraphrase.
2. Keep the core **platform-neutral** — neutral enough that any eligible platform's voice can be applied at the hub (an insight close for LinkedIn, an open-question close for Skool).

## OUTPUT
- `output/core-<slug>.md` (platform-neutral)

## GATE — human edits CRAFT (still high-edit)
Edit the prose: does it sound like you, is the texture right, is the close clean. **Do NOT re-decide the angle here.** If the angle feels wrong, the extraction was thin — **kick back to `01`** (re-collect / re-set direction), **not** to tier-3. Reasoning the idea afresh is a tier-3 act; this is production.

## NOT
- Do not invent or change the angle — it came from the `01` gate (`direction.md`).
- Do not write in a platform's voice yet (that's `03_adapt`). Keep the core forkable.
