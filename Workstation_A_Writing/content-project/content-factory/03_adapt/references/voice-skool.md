# voice-skool.md — Community-platform config — RESERVED (NOT POPULATED)
*The hub's second-platform seam. v1 of this example factory renders one platform only. This slot exists
so adding a second platform later does NOT force changes to `02_draft` or the hub's structure — only this
file fills in. (Replace "community platform" with whatever your second sink actually is.)*

---

## STATUS: RESERVED
Do **not** populate until the second platform is a **selected** sink at the 02→03 gate (state gates structure — no empty ceremony filled ahead of need).

## WHEN POPULATED (the seam's shape — not active config)
- **Posture:** a *different* posture from the first platform — e.g. peer/learner sharing work and seeking input, **not** broadcasting authority. The whole point of a second config is a genuinely different voice.
- **Close-form:** an **open question inviting peer input** (contrast the first platform's insight/reflection close).
- **Off-limits:** anything that isn't yours to post (others' IP, gated content) — name it explicitly.
- **Platform-only topics:** list any topics that suit this platform but not the first.
- **Dual-platform rule:** some posts work on both — **same body, different close**.

> [SEAM TEST] If filling this file later forces edits to `02_draft` (the core) or `03_adapt`'s structure, the pipeline→hub transition was *not* as clean as hypothesized — that's the finding. If only this file changes, the seam held.
