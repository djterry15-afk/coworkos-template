# CLAUDE.md
*Workstation: Writing — an EXAMPLE domain (a subject-area) for the things you write and publish.*
*Read ROOT CLAUDE.md fully before reading this. Read this fully before any project files.*

---

## WHAT THIS WORKSTATION IS FOR

*(Example.)* The domain where you produce things meant for an audience — posts, essays, newsletters,
talks. The reasoning about *what to say and why* lives here at tier-3; the repeatable *production* of a
piece lives in the tier-4 content factory beneath the project.

---

## PROJECT MAP

**content-project** — `Reasoning · Active`. The standing strategy/positioning for what you publish, plus a
tier-4 content factory (staged-pipeline + hub) that turns one idea into one publish-ready piece.
→ Folder: `content-project/`

**forming-idea** — `Forming (stub) · kind undetermined`. A just-started idea kept deliberately minimal (a map
+ a holding `CONTEXT.md`, nothing more) — the **stub** case. Here to show the **STATE** axis at its starting
point: *state gates structure*, so a stub earns no `references/`, `_reasoning-log/`, wiki, or `stages/` until
it goes Active and its kind is known. *(The Dead end of the same lifecycle is shown by
`Workstation_C_Workbench/retired-tool/`.)*
→ Folder: `forming-idea/`

---

## ROUTING TABLE

| Task | Navigate to | Load | Skip |
|---|---|---|---|
| Strategy / positioning for what to publish | content-project/ | This file + CONTEXT.md → project files | — |
| Producing a specific piece (a run) | content-project/content-factory/ | the factory's own files (production mode) | project synthesis |

---

## NAVIGATION SEQUENCE

1. ROOT CLAUDE.md (done) → 2. ROOT CONTEXT.md → 3. this file → 4. Writing CONTEXT.md →
5. the project's CLAUDE.md + CONTEXT.md → 6. begin work.
