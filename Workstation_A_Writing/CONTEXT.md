# CONTEXT.md
*Workstation: Writing. Domain posture delta + cross-project tensions only — not a synthesis.*
*Read this workstation's CLAUDE.md before this file.*

> **STOP:** if you haven't read Workstation_A_Writing `CLAUDE.md`, do that first.

---

## DOMAIN POSTURE (the delta over ROOT)

*(Example — replace.)* In this domain, prefer showing real work over claiming authority; hold any open
strategy question as unresolved rather than projecting a destination. Anything global (how you want the
agent to collaborate in general) stays at ROOT and is not repeated here.

## CROSS-PROJECT STATE

*(Thin by design.)* Only questions that span more than one project in this domain live here. With a single
example project, this stays near-empty — which is correct.
