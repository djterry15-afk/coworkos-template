# CLAUDE.md — vector-memory-platform (member)
*Member of: reference-study (Workbench). A reference sub-project — consult-from material studied for the
container's cross-member synthesis.*
*(EXAMPLE member — references a real public project; replace with your own. **Not vendored here** — read it
at the source.)*

---

## WHAT THIS IS
**cognee** (topoteretes) — an open-source AI-memory platform (Apache-2.0). Replaces RAG with an **ECL
pipeline** (Extract / Cognify / Load) into a knowledge graph + vector embeddings + relational metadata. The
**database-first counterexample** to the two markdown members.

## WHY IT'S HERE / WHAT TO EXTRACT
- The **counterexample**: a *successful* database-first memory system — so markdown-first is not the only
  viable answer.
- **Where** it differs: multi-agent / multi-tenant / hosted scale. The question to settle: does the *scale*
  explain the divergence (rather than contradicting the markdown members)?

## FINDINGS
→ this member's `CONTEXT.md`; compiled up into `../CONTEXT.md`.
