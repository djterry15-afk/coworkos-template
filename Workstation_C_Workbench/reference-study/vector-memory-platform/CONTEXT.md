# CONTEXT.md — vector-memory-platform (member findings)
*Written after study. What this member contributed to the cross-member synthesis (compiled up in `../CONTEXT.md`).*
*(EXAMPLE findings — illustrative.)*

---

- **A database-first memory platform that works** — graph + vector + relational, not markdown. So the
  markdown-first finding is a *scale-bounded* rule, not a universal law.
- **Its design center is multi-agent / multi-tenant / hosted scale** — exactly the regime the markdown
  members don't target.
- Same **pre-digest-on-ingest** instinct as the markdown members (ECL extracts at ingest), just filed into a
  graph instead of markdown — so the *mechanic* converges even where the *substrate* diverges.

→ The **counterexample** that sharpens, not breaks, the boundary. The container states the boundary in
`../CONTEXT.md`.
