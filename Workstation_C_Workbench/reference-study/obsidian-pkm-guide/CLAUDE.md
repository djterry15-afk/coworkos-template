# CLAUDE.md — obsidian-pkm-guide (member)
*Member of: reference-study (Workbench). A reference sub-project — consult-from material studied for the
container's cross-member synthesis.*
*(EXAMPLE member — references a real public project; replace with your own. **Not vendored here** — read it
at the source.)*

---

## WHAT THIS IS
A public guide to **Obsidian + Claude Code** as a personal knowledge system — a worked implementation of the
markdown-wiki pattern: `CLAUDE.md` + per-folder `index.md` as the primary interface, no MCP or vector DB
required.

## WHY IT'S HERE / WHAT TO EXTRACT
- A **worked implementation** of the markdown-first claim — does `CLAUDE.md` + `index.md` actually carry a
  real PKM in practice?
- The **"~80% without infrastructure"** claim, to weigh against the database-first member.

## FINDINGS
→ this member's `CONTEXT.md`; compiled up into `../CONTEXT.md`.
