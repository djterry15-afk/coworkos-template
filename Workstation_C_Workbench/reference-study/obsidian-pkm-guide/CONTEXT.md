# CONTEXT.md — obsidian-pkm-guide (member findings)
*Written after study. What this member contributed to the cross-member synthesis (compiled up in `../CONTEXT.md`).*
*(EXAMPLE findings — illustrative.)*

---

- **Confirms the markdown-first claim in practice** — a real PKM runs on `CLAUDE.md` + `index.md` as the
  interface, no vector store.
- **~80% of the value without infrastructure** — the remaining 20% (semantic search across a large base) is
  where a heavier tool would start to earn its place.
- Independent arrival at **CLAUDE.md-as-interface** — a second data point, not a one-off.

→ **Converges** with `markdown-wiki-pattern`: two independent sources land on markdown-first at personal
scale. See `../CONTEXT.md`.
