# CLAUDE.md — markdown-wiki-pattern (member)
*Member of: reference-study (Workbench). A reference sub-project — consult-from material studied for the
container's cross-member synthesis.*
*(EXAMPLE member — references a real public project; replace with your own. The project is **not vendored
here** — read it at the source; this folder holds only why it's here and what was extracted.)*

---

## WHAT THIS IS
Andrej Karpathy's **LLM Wiki** pattern — an LLM-maintained markdown knowledge base where sources are
**pre-digested into wiki pages on ingest** (no query-time RAG). Published as a public GitHub gist
(*cite the real URL when you add this for real; not vendored here*).

## WHY IT'S HERE / WHAT TO EXTRACT
- The **pre-digest-on-ingest** move: do the work when a source lands, not at query time.
- **CLAUDE.md as the schema** — the config file *is* the operation set (Ingest / Query / Lint).
- The claim to test against the other members: **plain markdown + an index is enough at personal scale.**

## FINDINGS
→ this member's `CONTEXT.md` (written after study); compiled up into `../CONTEXT.md`.
