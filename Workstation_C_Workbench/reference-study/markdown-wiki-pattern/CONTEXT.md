# CONTEXT.md — markdown-wiki-pattern (member findings)
*Written after study. What this member contributed to the cross-member synthesis (compiled up in `../CONTEXT.md`).*
*(EXAMPLE findings — illustrative.)*

---

- **Pre-digest-on-ingest is the core mechanic** — extraction happens at ingest, so a query is a cheap read.
  Substrate-independent: it files into markdown; a graph-based system files the same move into a graph.
- **Markdown + index suffices at personal scale** — the pattern claims no vector DB is needed until the base
  gets large.
- **CLAUDE.md-as-schema** recurs here — the folder's config file *is* the operation set, not just a map.

→ **Convergence** candidate with `obsidian-pkm-guide` (both markdown-first). **Tension** with
`vector-memory-platform` (database-first). The container reconciles these in `../CONTEXT.md`.
