# Retired Tool — Build Record
*(EXAMPLE static record — a finished, retired utility kept for reference only. Replace with your own.)*

---

**What it was:** a small file-aware CLI utility — read a workspace, build context, produce a planned output,
log the run. A complete, self-contained build.

**Final shape:** `CLI → validate workspace → list/read files → build context → plan → execute → log`.
Standard-library core, thin CLI shell.

**Why it's retired:** its job was absorbed by a later build and it is no longer needed. It reached "done,"
then went Dead — superseded, not abandoned mid-stream.

**Why it's kept:** a reference record — the shape is occasionally useful to consult; the code is not
maintained and nothing here is live.
