# CONTEXT.md
*Workstation: Workbench. Domain posture delta + cross-project tensions only — not a synthesis.*
*Read this workstation's CLAUDE.md before this file.*

> **STOP:** if you haven't read Workstation_C_Workbench `CLAUDE.md`, do that first.

---

## DOMAIN POSTURE (the delta over ROOT)

*(Example — replace.)* This domain is *support*, not active reasoning: the reference library is consulted,
the tool is built. Don't synthesize across the library (that would make it a Reasoning Workspace); don't
generate build plans for the tool unless asked.

## CROSS-PROJECT STATE

*(Thin by design.)* Only questions spanning both projects live here — e.g. if the tool ever starts
consuming the library, that dependency would be noted here.
